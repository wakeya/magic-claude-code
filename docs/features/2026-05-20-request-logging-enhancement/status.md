# Status

**状态:** shipped
**创建日期:** 2026-05-20
**完成日期:** 2026-05-20

## 说明

改进代理请求日志，为每个请求生成短 ID 并打印配对的入口（`>>>`）和出口（`<<<`）日志。入口包含路径、模型映射、消息/工具数量、请求体大小；出口包含状态码和上游耗时（含网络错误路径）。统一硬编码端点日志格式为 `[Hardcoded] Handling METHOD /path`。移除 `transformRequest` 中的重复模型映射日志。
